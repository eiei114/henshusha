# Basic example

A Henshusha workspace can contain multiple video projects under `projects/`.

Place media in `projects/sample-video/sources/raw/`, run the analyze-source skill for `projects/sample-video`, inspect `projects/sample-video/timelines/*.timeline.json`, then render.
